<h3>实现 (num1).add(num2).minus(num3)</h3>

```javascript
const isNumber = (num) =>
  Object.prototype.toString.call(num) === "[Object Number]";

Number.prototype.add = function(num) {
  const that = this;

  if (!isNumber(num)) {
    throw new Error("需要填入数字");
  }

  return that + num;
};

Number.prototype.minus = function(num) {
  const that = this;

  if (!isNumber(num)) {
    throw new Error("需要填入数字");
  }

  return that - num;
};
```
